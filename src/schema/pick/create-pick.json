{
  "title": "Create Pick API (Pick 생성)",
  "description": "velopick Pick 생성 API",
  "summary": "Pick",
  "tags": ["Pick"],
  "body": {
    "type": "object",
    "required": ["title"],
    "properties": {
      "title": { "type": "string" },
      "description": { "type": "string" },
      "is_private": { "type": "boolean" },
      "is_temp": { "type": "boolean" },
      "url_slug": { "type": "string" },
      "tags": { "type": "array", "items": { "type": "string" } },
      "users": { "type": "array", "items": { "type": "integer" } }
    }
  }
}
